{
  "query": "query TestFamilyAndChildren { me { id email firstName lastName } myFamilies { nodes { id name familyType createdBy memberCount childrenCount } totalCount } myChildren(first: 10) { edges { node { id name } cursor } pageInfo { hasNextPage hasPreviousPage startCursor endCursor totalCount } } }"
}