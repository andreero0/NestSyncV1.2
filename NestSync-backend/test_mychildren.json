{
  "query": "query MyChildren($first: Int, $after: String) { myChildren(first: $first, after: $after) { edges { node { id name dateOfBirth gender currentDiaperSize currentWeightKg currentHeightCm dailyUsageCount hasSensitiveSkin hasAllergies allergiesNotes onboardingCompleted province createdAt ageInDays ageInMonths weeklyUsage monthlyUsage } cursor } pageInfo { hasNextPage hasPreviousPage startCursor endCursor totalCount } } }",
  "variables": {
    "first": 10
  }
}