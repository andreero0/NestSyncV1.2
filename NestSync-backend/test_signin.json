{
  "query": "mutation SignIn($input: SignInInput!) { signIn(input: $input) { success message error user { id email firstName lastName } session { accessToken refreshToken expiresIn } } }",
  "variables": {
    "input": {
      "email": "parents@nestsync.com",
      "password": "Shazam11#"
    }
  }
}