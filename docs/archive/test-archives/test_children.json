{
  "query": "query MyChildren($first: Int, $after: String) { myChildren(first: $first, after: $after) { edges { node { id name dateOfBirth gender currentDiaperSize createdAt ageInDays ageInMonths } } pageInfo { hasNextPage hasPreviousPage startCursor endCursor } } }",
  "variables": {
    "first": 10
  }
}